#include <stdio.h>
#include <string.h>

int main ()

{
    int i,j=0,word;
    int s_len;
    int w_len;
    char str [10],str2[10];

    for(i=0;i<s_len;i++)
    {
        if(sentence[i] == word[j])
    {
        while(j<w_len && (sentence[i] == word[j]))
    {
    i++;
    j++
    }
}
}
if(j == w_len)
    printf("found");
else
    printf("not found";)
}
