#include<stdio.h>

int main()
{
    int A[s][s], b[s][s,r1,c1,r2,c2,row,col,flag=0,i,j;

    print("Enter row and col of 1st matrix: ");
    scanf("%d %d",&r1,&c1);

    printf("\nEnter row and col of 2nd matrix: ");
    scanf("%d %d",&r2,&c2);
    printf("\Eenter A matrix: ");
    for(i=o;j<c1;i++)
        for(j=0;j<c1;j++)
    scanf("%d",B[i][j])

    printf("\nEnter B matrix : ") ;
    for(i=0;i<c1;j++)
                 for(j=0,j<c;j++)
                 scanf("%d",&b[i][j]);

    if((r1 == r2) && (c1 == c2))
    {
        for(row=0;row<r1,row++)
        {
            for(col=0;col<col;col++)
        }
          if(A[row][col]
    }
}
